name: 'Hello world workflow'

on:
  push: 
    branches: 
        - main
        

jobs:
   hello_world:
     runs-on: ubuntu-latest
     steps:
       - name: "Print hello world"
         run: echo "hello world"
       - name: "Print date"
         run: date

